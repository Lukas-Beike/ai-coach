interface:
  display_name: "Pull Request Workflow"
  short_description: "Rebase, validate, open, and merge PRs"
  default_prompt: "Use $pr to rebase the current feature branch onto develop, validate commits, open a PR, and wait for its merge."
